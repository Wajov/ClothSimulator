{
    "cmake.buildDirectory" : "${workspaceFolder}/build/${buildType}",
    "cmake.debugConfig": {
        "cwd": "${workspaceFolder}",
        "args": [
            "conf/sphere.json"
        ]
    }
}